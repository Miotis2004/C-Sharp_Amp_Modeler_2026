<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Amp_Modeler_2026.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Amp_Modeler_2026"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Amp_Modeler_2026">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="20">
            <TextBlock Text="AmpModeler 2026" Style="{StaticResource TitleTextBlockStyle}" HorizontalAlignment="Center"/>

            <StackPanel Spacing="10">
                <TextBlock Text="Input Device:"/>
                <ComboBox ItemsSource="{Binding InputDevices}"
                          SelectedItem="{Binding SelectedInput, Mode=TwoWay}"
                          MinWidth="300"
                          DisplayMemberPath="Name"/>
            </StackPanel>

            <StackPanel Spacing="10">
                <TextBlock Text="Output Device:"/>
                <ComboBox ItemsSource="{Binding OutputDevices}"
                          SelectedItem="{Binding SelectedOutput, Mode=TwoWay}"
                          MinWidth="300"
                          DisplayMemberPath="Name"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="20" HorizontalAlignment="Center">
                <Button Content="Start Audio" Command="{Binding StartCommand}" Style="{StaticResource AccentButtonStyle}"/>
                <Button Content="Stop Audio" Command="{Binding StopCommand}"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" HorizontalAlignment="Center" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</Window>
